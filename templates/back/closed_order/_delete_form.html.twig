<form method="post" action="{{ path('app_back_closed_order_delete', { id: closed_order.id }) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
  <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ closed_order.id) }}" />
  <button class="btn btn-danger btn-sm mx-1 mb-2">Delete</button>
</form>
